# Ambition Bar - Pass 5

Substantive changes: **5** (tiny/focused CLI target: >= 5)

Dimensions touched: **6**

- agent_intuitiveness
- agent_ergonomics
- output_parseability
- error_pedagogy
- intent_inference
- self_documentation
- regression_resistance

Required surface types:

- Mega-command: pre-existing (`--robot-triage`), updated to mention CLI group replay.
- Capabilities or robot-docs: pre-existing, updated with `--group-candidates` and `[--json]` discovery command guidance.
- `--json` / robot output: pre-existing, extended with CLI group replay from JSON payloads.
- Error rewrite: added structured `invalid_group_followup` CLI JSON errors and `--mode` value suggestions.
- Intent-inference handler: added close-value suggestions for `--mode`.

Self-prompt round:

The initial batch had four substantive changes, so I applied the ambition check and added the fifth first-try discovery improvement for bare `capabilities` / `sources` before declaring the pass complete.

Bar met: **yes**.
