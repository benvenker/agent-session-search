# Code Mode Plan Review

You are the bounded review pass for the Code Mode plan council. Review the current plan against concept intent, CE-plan quality, required frontmatter, implementation readiness, and repo guardrails. Do not edit files.

REVIEW ITERATION:
{props.iteration}

PLAN PATH:
{props.planPath}

RESOLVED CONCEPT INPUT:
{props.concept}

SYNTHESIS METADATA:
{props.synthesis}

Review requirements:

- Read the plan file at `PLAN PATH`.
- Read the resolved concept document at `conceptPath` and skim the artifacts it indexes (digest brief, prototype findings, findings-council report).
- Check `AGENTS.md`, `DESIGN.md`, `CONTEXT.md`, `docs/adr/0001-fff-core-and-native-policy-strictness.md`, and `docs/plans/2026-07-16-002-feat-fff-two-lane-architecture-plan.md` (R12).
- Confirm the plan has frontmatter keys `artifact_contract`, `artifact_readiness`, `execution`, and `origin`.
- The plan MUST contain: an explicit R12 gate recommendation tied to the measured prototype evidence (not vibes); an explicit implementation-venue (worktree) recommendation; an evidence-maintenance section; and, if implementation is recommended, sequenced units with files/tests that respect FFF-as-core and keep server-side arbitrary code execution out of scope.
- Verify the R12 recommendation is consistent with the findings-council report's evidence-strength assessment — flag if the plan overclaims or underclaims relative to the council.
- Judge whether a coding agent could execute the plan (or execute the "defer/decline" decision's follow-ups) without another planning pass.

Approve only when those conditions hold. If not approved, return concrete issues and revision feedback.

REQUIRED OUTPUT:
{props.schema}

Return ONLY raw JSON matching the required output schema.
Do not include prose, markdown, headings, commentary, or code fences.
The first character of your response must be `{` and the last character must be `}`.
